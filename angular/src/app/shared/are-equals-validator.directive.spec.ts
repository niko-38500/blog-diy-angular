import { AreEqualsValidatorDirective } from './are-equals-validator.directive';

describe('AreEqualsValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AreEqualsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
